{
  "name": "neonhub-ai-ecosystem",
  "version": "0.2.0",
  "private": true,
  "description": "NeonHub AI Marketing Ecosystem - Self-operating, AI-driven marketing and sales platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist coverage .nyc_output && npm run clean --workspaces --if-present",
    "dev": "concurrently \"npm run dev:dashboard\" \"npm run dev:api\"",
    "dev:dashboard": "cd apps/dashboard && npm run dev",
    "dev:api": "cd apps/api && npm run dev",
    "db:generate": "npm run db:generate --workspace=@neon/data-model",
    "db:push": "npm run db:push --workspace=@neon/data-model",
    "db:migrate": "npm run db:migrate --workspace=@neon/data-model",
    "db:studio": "cd packages/data-model && npx prisma studio",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "ci": "npm run lint && npm run type-check && npm run test && npm run test:e2e",
    "type-coverage": "type-coverage",
    "test:ci": "npm test -- --ci --coverage --watchAll=false",
    "lint:ci": "npm run lint -- --max-warnings 0"
  },
  "keywords": [
    "ai",
    "marketing",
    "automation",
    "neonhub",
    "production-ready",
    "ai-marketing",
    "ecosystem",
    "langchain",
    "openai",
    "nextjs",
    "trpc"
  ],
  "author": "NeonHub Team",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "@playwright/test": "^1.53.1",
    "@types/jest": "^30.0.0",
    "@typescript-eslint/eslint-plugin": "^8.34.1",
    "@typescript-eslint/parser": "^8.34.1",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^10.1.5",
    "jest": "^30.0.2",
    "playwright": "^1.53.1",
    "prettier": "^3.5.3",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "stripe": "^18.2.1"
  }
}
